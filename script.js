/** @format */

$("#audio-control").click(function () {
  if ($("#video-bg").prop("muted")) {
    $("#video-bg").prop("muted", false);
    // $(this).text("Mute");
    // or toggle class, style it with a volume icon sprite, change background-position
    // $(this).html("<span><i class='fas fa-volume-up'></i></span>");
  } else {
    $("#video-bg").prop("muted", true);
    // $(this).text("Unmute");
    // $(this).html("<span><i class='fas fa-volume-mute'></i></span>");
  }
});

$("#audio-control").click(function () {
  if ($("#video-bg").prop("muted")) {
    $("#video-bg").prop("muted", false);
    // $(this).text("Mute");
    // or toggle class, style it with a volume icon sprite, change background-position
    // $(this).html("<span><i class='fas fa-volume-up'></i></span>");
  } else {
    $("#video-bg").prop("muted", true);
    // $(this).text("Unmute");
    // $(this).html("<span><i class='fas fa-volume-mute'></i></span>");
  }
});

$("#audio-control").click(function () {
  if ($("#video-bg").prop("muted")) {
    $("#video-bg").prop("muted", false);
    // $(this).text("Mute");
    // or toggle class, style it with a volume icon sprite, change background-position
    // $(this).html("<span><i class='fas fa-volume-up'></i></span>");
  } else {
    $("#video-bg").prop("muted", true);
    // $(this).text("Unmute");
    // $(this).html("<span><i class='fas fa-volume-mute'></i></span>");
  }
});
